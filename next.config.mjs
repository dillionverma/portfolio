/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;

module.exports = {
  output: 'export',
  // Base path for GitHub Pages
  basePath: '/robbieradev.github.io/',
  // Asset prefix for GitHub Pages
  assetPrefix: '/robbieradev.github.io/',
}
